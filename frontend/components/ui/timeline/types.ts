export interface VTimeLineProps {
    horizontal?: boolean,
}

export interface VTimeLineItemProps {
    index?: number;
    progress?: number;
    active?: boolean,
    complete?: boolean,
    horizontal?: boolean,
}
